<%@ page language="java" contentType="text/html; charset=UTF-8" pageEncoding="UTF-8"%>
<%@ page import="java.sql.*" %>
<%@ include file="DBconnection.jsp" %>
<%@ include file="sessionManager.jsp" %>

<%
String dbName = (session != null) ? (String) session.getAttribute("dbName") : null;
String id = (session != null) ? (String) session.getAttribute("id") : null;

if (id == null || dbName == null) {
	response.sendRedirect("login.jsp");
	return;
}

jdbcDriver = dbName;
String medicineName = request.getParameter("medicineName");

try {
    Class.forName("com.mysql.cj.jdbc.Driver");
    String sql = "SELECT standard, buyingPrice, price FROM testTable WHERE medicineName = ?";
    PreparedStatement pstmt = conn.prepareStatement(sql);
    pstmt.setString(1, medicineName);
    ResultSet rs = pstmt.executeQuery();
%>
	<table style="width: 100%; border-collapse: collapse;">
		<tr style="background-color:#f5f5f5;">
			<th>규격</th><th>사입가</th><th>판매가</th>
		</tr>
	<%
	while (rs.next()) {
	%>
		<tr>
			<td><input type="text" class="standards" value="<%=rs.getString("standard")%>" style="width: 100%;"></td>
			<td><input type="number" class="standards" value="<%=rs.getDouble("buyingPrice")%>" style="width: 100%;"></td>
			<td><input type="number" class="standards" value="<%=rs.getDouble("price")%>" style="width: 100%;"></td>
		</tr>
	<%
	}
	%>
	</table>
<%
} catch (Exception e) {
	out.println("<p>에러 발생: " + e.getMessage() + "</p>");
} finally {
    if (conn != null) conn.close();
}
%>
