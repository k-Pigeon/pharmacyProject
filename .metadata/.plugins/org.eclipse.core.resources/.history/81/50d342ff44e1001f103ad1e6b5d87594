import java.sql.*;

import javax.swing.*;

public class MainFrame extends JFrame {
    public MainFrame() {
        setTitle("메인 화면");
        setBounds(600, 300, 400, 300);
        setLayout(null);

        JLabel welcomeLabel = new JLabel("로그인 성공! 메인 화면입니다.");
        welcomeLabel.setBounds(100, 100, 200, 30);
        add(welcomeLabel);

        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        setVisible(true);
    }
}